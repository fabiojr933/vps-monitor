<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>VPS Monitor v1.0.0</title>

  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<main class="main-container container-fluid">

<h1 class="vps-title">• VPS Monitor <small>v1.0.0</small></h1>

<div class="row g-4">

<div class="col-md-6 vps-card">
  <h5><i class="bi bi-cpu"></i> CPU</h5>
  <div class="stats-row"><span>Modelo</span><span><%= data.cpu.model %></span></div>
  <div class="stats-row"><span>Cores / Threads</span><span><%= data.cpu.cores %> / <%= data.cpu.threads %></span></div>
  <div class="stats-row"><span>Load</span><span><%= data.cpu.load %></span></div>

  <div class="progress-custom">
    <div class="progress-bar-inner" style="width:<%= data.cpu.usage %>%"></div>
  </div>
</div>

<div class="col-md-6 vps-card">
  <h5><i class="bi bi-memory"></i> MEMÓRIA</h5>
  <div class="stats-row"><span>Total</span><span><%= data.memory.total %> GB</span></div>
  <div class="stats-row"><span>Usado</span><span><%= data.memory.used %> GB</span></div>
  <div class="stats-row"><span>Livre</span><span><%= data.memory.free %> GB</span></div>

  <div class="progress-custom">
    <div class="progress-bar-inner" style="width:<%= data.memory.percent %>%"></div>
  </div>
</div>

<div class="col-12 vps-card">
  <h5><i class="bi bi-hdd"></i> SISTEMA</h5>
  <div class="stats-row"><span>Hostname</span><span><%= data.system.hostname %></span></div>
  <div class="stats-row"><span>OS</span><span><%= data.system.os %></span></div>
  <div class="stats-row"><span>Arquitetura</span><span><%= data.system.arch %></span></div>
  <div class="stats-row"><span>IP Externo</span><span class="text-neon"><%= data.system.ipExternal %></span></div>
  <div class="stats-row"><span>Uptime</span><span><%= data.system.uptime %></span></div>
</div>

<div class="col-12 vps-card">
  <h5><i class="bi bi-shield-lock"></i> PORTAS</h5>

  <% data.ports.forEach(p => { %>
    <div class="port-row">
      <div class="port-number"><%= p.port %></div>
      <div class="port-name"><%= p.name %> (<%= p.type %>)</div>
      <div class="status-badge online">ONLINE</div>
    </div>
  <% }) %>
</div>

</div>

</main>

</body>
</html>
